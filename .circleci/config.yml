version: 2.1
orbs:
  python: circleci/python@2.1.1

jobs:
  run_tests:
    machine:
      image: ubuntu-2004:202201-02
    steps:
      - checkout

      - python/install-packages:
          pip-dependency-file: server/requirements.txt
          pkg-manager: pip

      - run:
          name: Run file
          command: python3 server/tests.py

      - run:
          name: List files
          command: cd server; ls -lR | more

      - run:
          name: Run tests
          command: python3 -m pytest server/tests.py

  build_app:
    docker:
      - image: cimg/python:3.8
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: Docker build
          command: docker-compose build
      - run:
          name: Authenticate with Docker Hub
          command: echo "$DOCKERHUB_PASS" | docker login --username $DOCKERHUB_USERNAME --password-stdin
      - run:
          name: Push Docker Image to Docker Hub
          command: docker-compose push

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - run_tests:
          filters:
            branches:
              only:
                - main
                - docker-pipeline
      - build_app:
          filters:
            branches:
              only:
                - main
                - docker-pipeline
          requires:
            - run_tests
